# Shared function secrets (set via supabase secrets set)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
CRYPTO_KEY=

# Internal trigger security token (must match DB trigger config)
INTERNAL_EDGE_TRIGGER_TOKEN=

# Optional
APP_BASE_URL=
SMTP_TIMEOUT_MS=10000
